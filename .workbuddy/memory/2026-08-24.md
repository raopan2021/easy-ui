# 2026-08-24

## 组件库拆分重构进度（packages/components）

目标模式：组件拆为 `xxx.ts`(props/emits) + `use-xxx.ts`(逻辑 composable) + `xxx-style.scss`(样式) + `.vue`(模板组合)。参考样板：switch 组件四件套。

- 组件总数：69
- 截至本日已拆分（含 use*.ts）：52（含此前已拆 49 + 今日新增 3）
- 待拆分：16

### 待拆分组件（按规模/优先级，从易到难）

- 薄包装/小型：form(340/8)、richtext(242/12)
- 中型：user-picker(824/22)、china-map(935/42)、time-picker(698/43)、date-picker(699/37)、flow-designer(664/36)
- 大型：file-preview(1098/24)、image-upload(1036/57)、file-upload(1296/24)、cascader(1258/56)、gantt(1479/46)、date-time-picker(1041/57)
- 超大型：video(1923/78)、chart(2612/132)、table(3294/87)

（格式：文件行数 / 内联逻辑符号数）

## 第一批（薄包装 range-picker）已完成

拆分：date-range-picker、date-time-range-picker、time-range-picker → 各 ts + use + scss + 精简 vue。
验证口径发现：

- 单独 `eslint` 这 3 个组件 = 0 error。
- 组件库 `type-check` 整体报错，但全来自**既有已拆组件** tour / tree-chart / worktab（拆分遗留类型 bug，非本次引入）；本次 3 组件未出现在报错。
- 全量 `lint` 160 问题多在 template-admin（既有）。
  => 项目整体 lint/type-check 当前非绿，是"重构进行中"状态；验证应聚焦"本次改动自身 lint+type 干净、不引入新错误"。

## 任务清单（TaskCreate #1-#10，从易到难分档，每档做完验证）

# 1 薄包装 range-picker(3) —— 已完成

# 2 form、richtext

# 3 user-picker、china-map

# 4 time-picker、date-picker

# 5 flow-designer

# 6 file-preview、image-upload

# 7 file-upload、cascader

# 8 gantt、date-time-picker

# 9 video、chart

# 10 table
